# 📁HTTP Protocol (Basic & Advanced)

---

## 1. HTTP Method 및 멱등성 (Idempotency)

#### 1) GET vs POST
* **GET**: 서버의 리소스를 **조회**할 때 사용. 요청 데이터가 URL(Query Parameter)에 노출됨. **캐싱 가능** (가장 큰 차이).
* **POST**: 서버에 리소스를 **생성(Create)** 하거나 데이터를 처리할 때 사용. 요청 데이터가 Body에 담김. **캐싱 불가능**.

#### 2) PUT vs PATCH (가장 중요한 차이: 멱등성)
> **멱등성(Idempotency)**이란?  
> 동일한 요청을 한 번 보내든, 여러 번 연속으로 보내든 **서버의 상태 결과가 항상 동일**한 성질.

| Method | 역할 | 멱등성 | 설명 |
| :---: | :--- | :---: | :--- |
| **PUT** | 리소스 **전체 수정** (덮어쓰기) | **O** | 리소스 전체를 Payload로 교체하므로, 100번 요청해도 결과는 같다. |
| **PATCH** | 리소스 **부분 수정** | **X** | `amount + 10` 같은 연산은 호출할 때마다 값이 변하므로 멱등하지 않다. |

#### 3) DELETE
* 리소스를 **삭제**한다.
* **멱등성 O**: 삭제된 리소스를 다시 삭제하려 해도 결과 상태(삭제됨)는 동일하다.

---

## 2. HTTP Status Code

#### 2xx: 성공 (Success)
* **200 OK**: 요청 성공.
* **201 Created**: 생성 성공. (POST)
* **204 No Content**: 성공했으나 Body가 없음. (DELETE)

#### 4xx: 클라이언트 오류 (Client Error)
* **400 Bad Request**: 파라미터/문법 오류.
* **401 Unauthorized**: **인증** 실패 (Login 필요).
* **403 Forbidden**: **권한** 부족 (접근 불가).
* **404 Not Found**: 리소스 없음.
* **405 Method Not Allowed**: 허용되지 않는 메서드.
* **409 Conflict**: 리소스 상태 충돌 (중복 생성 등).

#### 5xx: 서버 오류 (Server Error)
* **500 Internal Server Error**: 서버 내부 로직 에러.
* **502 Bad Gateway**: 게이트웨이 오류.
* **503 Service Unavailable**: 서버 과부하/점검.
* **504 Gateway Timeout**: 시간 초과.

---

## 3. HTTP Headers

#### 1) 공통/표준 헤더
* **Content-Type**: Body의 MIME Type (`application/json`, `text/html`).
* **Authorization**: 인증 토큰 (`Bearer <access-token>`).

#### 2) 성능 및 캐싱 (중요)
* **ETag / If-None-Match**:
    * 리소스의 해시값(버전)을 비교하여 변경 사항이 없으면 `304 Not Modified` 응답.
    * 대역폭을 획기적으로 절약.
* **Cache-Control**: `no-cache` (매번 서버 확인), `no-store` (저장 안 함), `max-age` (유효 시간).

#### 3) 안정성
* **Idempotency-Key**: 중복 결제 방지용 고유 키 헤더.

---

## 4. HTTP 심화 (Junior Interview 필수)

#### 1) Connectionless & Stateless
* **Connectionless**: 요청 하나를 처리하면 연결을 끊는다. (서버 자원 효율)
* **Stateless (무상태)**: 서버는 클라이언트의 이전 상태를 기억하지 않는다.
    * **단점**: 매번 토큰/쿠키를 보내야 함.
    * **장점**: 서버 확장(Scale-out)이 자유로움.

#### 2) Keep-Alive (Persistent Connection)
* Connectionless의 비효율(매번 3-way handshake)을 줄이기 위해, 일정 시간 동안 **TCP 연결을 재활용**하는 기술.
* HTTP/1.1부터 기본 활성화(`Connection: keep-alive`).

#### 3) HTTPS (HTTP Secure)
* HTTP에 **SSL/TLS 암호화 계층**을 추가한 프로토콜.
* **동작 원리 (간략)**:
    1. 서버가 **공개키(Public Key)** 인증서를 클라이언트에 줌.
    2. 클라이언트는 데이터를 **대칭키**로 암호화하고, 그 대칭키를 **공개키**로 잠궈서 보냄.
    3. 서버는 **개인키(Private Key)**로 대칭키를 복호화해서 통신 시작.
